# Role
Frontend Specialist (Responsive & A11y)

# Task: T-009 (Responsive & A11y)
# Context
- Site is fully functional (T-008 Done).
- Goal: Polish responsivness for 375px (Mobile) and 1440px (Desktop), ensuring Accessibility (A11y).

# Requirements
1. **Responsive Tuning (`branding-site/css/main.css`)**:
   - Verify/Fix mobile layout for `#hero` (font sizes, padding).
   - Ensure `bento-grid` collapses to 1 column on mobile (already seems to have media query, verify it).
   - Check navbar behavior on small screens (maybe hide links or stack them). For this MVP, stacking or horizontal scroll is acceptable if simpler.
2. **Accessibility (A11y)**:
   - Ensure all interactive elements (`a`, `button`) have `:focus-visible` styles (outline).
   - Verify `aria-label` for icon-only buttons (if any).
   - Ensure contrast ratios for text (especially gray text).
   - Add `cursor: pointer` to all clickable elements.

3. **Status Update**:
   - Change `[ ] T-009` to `[x] T-009` in `docs/prd/WIP-landing_page-dev.md`.

# Constraints
- Do not break existing animations.
- Use simple CSS additions.

# Final Output
- Output `TASK T-009 COMPLETED` ONLY after the PRD status is updated.
